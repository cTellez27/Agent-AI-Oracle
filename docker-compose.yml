version: '3.8'

services:
  agent-ai-oracle:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agent_ai_oracle_app
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
